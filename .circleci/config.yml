# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    docker:
      - image: cimg/go:1.16

    working_directory: /tmp/metahelm
    steps:
      - checkout
      #- run: go get -v -t -d ./...
      - run: go test -v ./...
      - run: go vet ./...
